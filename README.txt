The UNIX script file_remove.ksh uses KORN Shell to find and removes the file based on
directory_path days_old file_name

Copy / create file_remove.cfg file into $HOME/config/ path.

When the shell script file_remove.ksh is executed, it will create file_remov.log in /tmp path. 